# Harness Spec — CLI edge cases

## Context
False-positive fixture for validate_harness.py's CLI self-description check.

## Component specs
- `.claude/skills/edge-skill/` — holds argparse shapes the check must stay silent on.
