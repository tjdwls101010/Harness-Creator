---
name: dead-link-skill
description: A skill that references files that don't exist, to test dead-link detection.
---

# Dead link skill

See `references/nonexistent.md` and `scripts/nonexistent.py` for details that do not actually exist on disk.
