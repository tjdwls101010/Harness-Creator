# Frontend style

No `paths:` frontmatter, and nested one level down. This file loads at launch
with the same priority as `.claude/CLAUDE.md`, so it belongs in the
always-loaded budget and in the rules inventory.
